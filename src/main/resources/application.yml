# Spring Boot Configuration

server:
  http2:
    enabled: true
  port: 8443
  ssl:
    key-store: classpath:app-combined.p12
    key-store-password: secret
  forward-headers-strategy: native

logging.level.app: debug
